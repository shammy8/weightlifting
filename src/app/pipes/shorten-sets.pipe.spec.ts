import { ShortenSetsPipe } from './shorten-sets.pipe';

describe('ShortenSetsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenSetsPipe();
    expect(pipe).toBeTruthy();
  });
});
